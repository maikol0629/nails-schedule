// Página mantenida por compatibilidad con el flujo anterior de verificación
// Actualmente, la verificación con código se realiza directamente en StylistRegister (paso 2).

export default function VerifyWhatsApp() {
  return null;
}
